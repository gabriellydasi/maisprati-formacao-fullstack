const prompt = require('prompt-sync')() // utilizado para facilitar a entrada de dados pelo usuário no console em programas Node.js. Ele permite que você solicite e capture a entrada do usuário de forma síncrona, ou seja, o programa espera até que o usuário insira um valor e pressione Enter.

// //<variável>;<condição>;<incremento>

// let counter = 1
// let multiplier = parseInt(prompt('Informe o número que você quer saber a tabuada: '))

// for(counter = 1; counter <= 10; counter++){
//     console.log(`${multiplier} x ${counter} =`, multiplier * counter)
// }

// for(let i = 0; i <= 20; i += 2){
//     console.log(i)
// }

